# Slack App Credentials
# Get these from https://api.slack.com/apps
SLACK_CLIENT_ID=your-slack-client-id
SLACK_CLIENT_SECRET=your-slack-client-secret
SLACK_SIGNING_SECRET=your-slack-signing-secret

# ServiFlow API Configuration
SERVIFLOW_API_URL=https://web-production-11114.up.railway.app
SERVIFLOW_URL=https://serviflow.app
SERVIFLOW_TENANT_CODE=apoyar

# Database Configuration (same as main app)
MYSQLHOST=localhost
MYSQLPORT=3306
MYSQLUSER=root
MYSQLPASSWORD=your-mysql-password
MYSQLDATABASE=master_tenant

# Webhook Security
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SLACK_WEBHOOK_SECRET=your-webhook-secret

# Server Configuration
PORT=3979
NODE_ENV=development
