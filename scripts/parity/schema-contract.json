{
  "description": "Schema contract for UAT/Production parity verification",
  "tenant": "apoyar",
  "required_tables": [
    "tickets",
    "ticket_activity",
    "cmdb_items",
    "tenant_settings",
    "email_ingest_settings",
    "ai_email_analysis"
  ],
  "required_columns": {
    "tickets": [
      "work_type",
      "execution_mode",
      "system_tags"
    ],
    "email_ingest_settings": [
      "enabled"
    ]
  },
  "required_settings": {
    "_comment": "tenant_settings uses key-value rows, not columns",
    "tenant_settings": [
      "process_emails"
    ]
  }
}
