# Microsoft Bot Framework Credentials
# Get these from Azure Portal > App Registrations
MICROSOFT_APP_ID=your-microsoft-app-id
MICROSOFT_APP_PASSWORD=your-microsoft-app-password
MICROSOFT_APP_TENANT_ID=your-microsoft-tenant-id

# ServiFlow API Configuration
SERVIFLOW_API_URL=https://web-production-11114.up.railway.app
SERVIFLOW_URL=https://serviflow.app
SERVIFLOW_TENANT_CODE=apoyar

# Service Account for API calls (create a dedicated user in ServiFlow)
SERVIFLOW_SERVICE_USER=teams-bot
SERVIFLOW_SERVICE_PASSWORD=your-service-account-password

# Webhook Security
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TEAMS_WEBHOOK_SECRET=your-webhook-secret

# Server Configuration
PORT=3978
NODE_ENV=development
